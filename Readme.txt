Step 1:
run "npm install" to install necessary node modules

Step 2:
run "npm start" to run the project in default browser

Step 3:

go to path "https:/localhost:300/home" to see the grid/card view
go to path "https://localhost:300/add" to add a new user to the object